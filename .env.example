# 小红书运营技能 - 环境变量配置
# 复制此文件为 .env 并填入你的凭据
# cp .env.example .env
#
# ⚠️ 登录认证优先级：
#   1. Profile 持久化登录态（~/.xhs-skill/profiles/）— 首选，自动跨会话复用
#   2. 本文件中的 XHS_COOKIE — 仅当 profile 失效时降级使用
#   3. QR 扫码登录 — 最终兜底，登录后 profile 自动持久化
#
# 推荐流程：首次运行 `python scripts/browser.py login` 扫码登录即可，
# 后续启动自动复用 profile，无需手动维护 XHS_COOKIE。

# 即梦API Session ID
# 获取方式：jimeng.jianying.com → F12 → Application → Cookies → sessionid
# 国际站加前缀：us-/hk-/jp-/sg-
JIMENG_SESSION_ID=""

# 即梦API 地址（默认本地 Docker）
JIMENG_API_URL="http://localhost:5100"

# 小红书域名（国际版用 rednote.com，国内版用 xiaohongshu.com）
# 影响 Cookie domain、登录页 URL、首页检测等
XHS_DOMAIN="xiaohongshu.com"

# 小红书 Cookie（降级兜底用，profile 失效时才读取）
# 获取方式：Chrome 打开对应域名并登录 → F12 → Network → 任意请求 → Request Headers → Cookie → 复制整行值
# ⚠️ Cookie 值必须用双引号包围（值中含 ; { } % 等特殊字符）
# 💡 通常不需要手动填写：扫码登录后 profile 自动保存登录态
XHS_COOKIE=""
